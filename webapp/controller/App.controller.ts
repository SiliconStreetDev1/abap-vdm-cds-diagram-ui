import Controller from "sap/ui/core/mvc/Controller";

/**
 * @namespace nz.co.siliconstreet.vdmdiagrammer.controller
 * @fileoverview Root App Controller.
 * @description Manages the absolute root `<App>` view that initializes the Fiori Shell.
 */
export default class App extends Controller {

    /**
     * @public
     * @description Lifecycle initialization.
     */
    public onInit(): void {

    }
}